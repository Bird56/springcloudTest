server:
  port: 8050

spring:
  application:
    name: feign

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

# 开启熔断机制
feign:
  hystrix:
    enabled: true