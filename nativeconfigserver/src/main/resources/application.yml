server:
  port: 8762

spring:
  application:
    name: nativeconfigserver
#    配置文件获取方式 这里本地
  profiles:
    active: native
#    配置文件存放路径
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared

